# Sources Configuration
# Optional: define entry files and scan paths for knowledge base extraction.

git_commit: "PLACEHOLDER"

# Entry files — where the skill starts scanning
entry_files:
  # - "src/main.ts"

# Scan paths — directories to search for model/entity/controller definitions
scan_paths:
  # - "src/models/"
  # - "src/entities/"
  # - "migrations/"

# Patterns — glob patterns for files containing entity definitions
# scan_patterns:
#   - "**/*.model.ts"
#   - "**/*.entity.ts"
